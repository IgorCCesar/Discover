//Função que eleva o número ao quadrado

// Imperativo: Faça da seguinte forma

let number = 2;

function square() {
  return number * number;
}

number = square();

// Declarativa: O que fazer e não como fazer
const square = (n) => n * n; // Na EuroFunction quando temos apenas uma linha ela retorna a função automaticamente
